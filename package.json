{
  "name": "gh-migrate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite dev",
    "build": "bunx --bun svelte-kit sync && bunx --bun vite build && bun scripts/postbuild.ts",
    "preview": "bunx --bun vite preview",
    "typecheck": "bunx --bun tsc --noEmit && bunx --bun tsc --noEmit -p scripts/tsconfig.json",
    "check": "bunx --bun svelte-kit sync && bunx --bun svelte-check --tsconfig ./tsconfig.json --fail-on-warnings",
    "lint": "bunx --bun biome lint --error-on-warnings .",
    "format": "bunx --bun biome format --write .",
    "format:check": "bunx --bun biome format .",
    "test": "bun test",
    "coverage:check": "bun scripts/check-coverage.ts",
    "audit": "bun audit",
    "dup": "jscpd",
    "deadcode": "knip",
    "cycles": "madge --circular --extensions ts,svelte --ts-config tsconfig.json src",
    "mutation": "stryker run",
    "ci": "bun run check && bun run build",
    "seed": "bun scripts/seed.ts",
    "verify": "bun run typecheck && bun run check && bun run lint && bun run format:check && bun run coverage:check && bun run dup && bun run deadcode && bun run cycles && bun run build && bun run audit",
    "verify:json": "bun scripts/verify-json.ts"
  },
  "dependencies": {
    "@octokit/auth-app": "^8.2.0",
    "@octokit/graphql": "^9.0.3",
    "@octokit/plugin-retry": "^8.1.0",
    "@octokit/plugin-throttling": "^11.0.3",
    "@octokit/rest": "^22.0.1",
    "@primer/octicons": "^19.28.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@stryker-mutator/core": "^9.6.1",
    "@sveltejs/adapter-node": "^5.5.4",
    "@sveltejs/kit": "^2.64.0",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tailwindcss/vite": "^4.3.0",
    "@types/bun": "^1.3.14",
    "@types/primer__octicons": "^19.21.0",
    "jscpd": "^5.0.5",
    "knip": "^6.16.1",
    "madge": "^8.0.0",
    "svelte": "^5.56.3",
    "svelte-check": "^4.6.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "overrides": {
    "picomatch": "^4.0.4",
    "cookie": "^0.7.2",
    "qs": "^6.15.2"
  }
}
